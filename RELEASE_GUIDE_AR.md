# تسليم أساس LIMS — الإصدار 7.4.1

## القنوات المتاحة

| القناة | المصدر أو الملف | الحالة |
|---|---|---|
| Windows (تشغيل محلي) | `تشغيل_نظام_أساس.bat` أو `start-local.ps1` | جاهز لتشغيل خادم Python محلياً |
| المتصفح / PWA | `index.html` و`manifest.webmanifest` والخادم | جاهز عند تشغيل الخادم؛ يمكن تثبيته من المتصفح كتطبيق |
| Android الميداني | `LIMS_Asas_Integrated_V7.0.0_BASE_READY/field` | مصدر Android جاهز للبناء؛ `versionName` هو `7.4.1` و`versionCode` هو `19` |
| إنتاج سحابي | `render.yaml` مع `runtime-config.js` | يحتاج ضبط أسرار الاستضافة والتحقق من النشر |

## تشغيل Windows

1. ثبّت Python 3.11 أو أحدث.
2. شغّل `تشغيل_نظام_أساس.bat`، أو `start-local.ps1` إذا رغبت في إدخال رقم المدير وكلمة المرور أثناء البدء.
3. افتح العنوان الذي يطبعه الخادم؛ المنفذ الافتراضي هو `8080` ويمكن تغييره بواسطة `LIMS_PORT`.

## إنشاء حزمة Android

1. افتح مجلد `LIMS_Asas_Integrated_V7.0.0_BASE_READY/field` في Android Studio.
2. نفّذ `gradlew.bat assembleDebug` للاختبار، أو عرّف ملف توقيع محلي ثم نفّذ `gradlew.bat assembleRelease` للإصدار.
3. اختبر APK الناتج على جهاز فعلي قبل التوزيع. لا يوجد APK موقّع ضمن هذا المستودع ولا ينبغي وضع مفاتيح التوقيع أو كلمات المرور في Git.

## بيانات الموقع الرسمي والخصوصية

واجهة «عن مختبر أساس» تستعمل فقط معلومات عامة منشورة في [الموقع الرسمي](https://www.asaslab.com/): الخدمات، الفروع، التعريف العام، ونماذج المشاريع. لا تُنشأ سجلات في جدول العملاء من أسماء أو أرقام أو مراسلات منشورة على الإنترنت؛ استيراد العملاء يتطلب ملفاً معتمداً من إدارة المختبر وموافقة على استخدام البيانات.

## التحقق قبل التسليم

- شغّل `python -m unittest test_server.py`.
- افتح النظام على Windows وتحقق من الدخول والمشاريع والعينات والتقارير.
- ابنِ Android وجرّب المزامنة على جهاز فعلي؛ نجاح بناء APK لا يثبت وحده صحة التشغيل أو الربط المركزي.
