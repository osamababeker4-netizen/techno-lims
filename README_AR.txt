مختبر أساس LIMS — الإصدار V7.4.1

هذه نسخة عربية RTL لإدارة المختبر والمشاريع، مع المحافظة على البرنامج الميداني المحلي وواجهة API المركزية القابلة للربط. تتضمن هوية لونية وتدفق عمل مستندين إلى المعلومات العامة المنشورة في موقع مختبر أساس الرسمي.

أبرز القدرات:
- لوحة قيادة تشغيلية للأولويات والعوائق والمراجعات وطابور المزامنة.
- إدارة المشاريع بطرق عرض جدول وKanban وخارطة طريق.
- ربط المشروع بأوامر العمل والعينات والاختبارات والنتائج والتقارير والزيارات الميدانية.
- مستخدمون وأدوار وصلاحيات وسجل تدقيق.
- دورة مراجعة واعتماد للمشاريع والزيارات والتقارير.
- ترحيل قاعدة SQLite غير هدّام لقواعد البيانات القائمة.
- طابور مزامنة محلي يحافظ على نهج Local-First قبل الربط مع Central API.
- تصميم متجاوب لسطح المكتب والجوال.

التشغيل المحلي:
1. عيّن متغير البيئة LIMS_BOOTSTRAP_PASSWORD محلياً إلى كلمة مرور قوية من 12 حرفاً أو أكثر قبل أول تشغيل.
2. شغّل: python server.py
3. افتح: http://127.0.0.1:8080
4. بعد أول دخول، أنشئ المستخدمين والأدوار اللازمة من شاشة المستخدمين والصلاحيات.

لا تضع كلمات مرور أو API keys أو ملفات قاعدة بيانات أو إعدادات بيئية في Git.
لا تستخدم ملف ZIP كمصدر للتطوير؛ ملفات المصدر الفعلية هي HTML وCSS وJavaScript وPython وSQL في جذر المشروع.

النشر الإنتاجي:
- ملف render.yaml يجهز خدمة Render بقرص دائم واحد لقاعدة SQLite. يطلب Render قيمة LIMS_BOOTSTRAP_PASSWORD السرية عند إنشاء الخدمة ولا يحفظها في Git.
- بعد ظهور عنوان خدمة Render، عيّن LIMS_ALLOWED_ORIGIN إلى https://osamababeker4-netizen.github.io، وضع العنوان نفسه في runtime-config.js ضمن LIMS_API_BASE_URL ثم أعد نشر GitHub Pages.
- هذا النشر مقصود لمثيل واحد بسبب SQLite والقرص الدائم. عند الحاجة إلى توسع أو توفر عالٍ، تُنفَّذ هجرة معتمدة إلى قاعدة بيانات مركزية قبل تفعيل عدة مثيلات.

ملاحظة اعتماد:
يلزم للاعتماد الرسمي ISO/IEC 17025 أو SAAC تنفيذ IQ/OQ/PQ، التحقق من طرق الاختبار والحسابات، إجراءات التشغيل، النسخ الاحتياطي والاستعادة، اختبار الأمن، ربط الأجهزة، والتحقق من Central API في بيئة معتمدة.
## تحقق SMS عبر Twilio Verify

تدعم واجهة API المركزية في هذا المستودع التحقق الحقيقي عبر SMS للتطبيق الميداني:

1. أنشئ خدمة Verify في Twilio وأكمل ملف الامتثال الأساسي.
2. في إعدادات الاستضافة فقط، أضف `TWILIO_ACCOUNT_SID` و`TWILIO_AUTH_TOKEN` و`TWILIO_VERIFY_SERVICE_SID`.
3. حدّث رقم جوال كل مستخدم من شاشة المستخدمين بصيغة E.164، مثال: `+9665XXXXXXXX`.
4. من تطبيق Android، أدخل رابط الخادم HTTPS في «المزامنة المركزية»، ثم اطلب OTP وتحقق واربط الحساب.

لا تضع رموز Twilio في `app.js` أو تطبيق Android أو GitHub. صفحات GitHub Pages ثابتة ولا يمكنها إرسال SMS مباشرة.
